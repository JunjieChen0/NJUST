/**
 * Central home for prompt-related numeric defaults (report P2 / F follow-ups).
 * Incrementally migrate magic numbers from sections into these exports.
 */
export const PROMPT_DEFAULTS = {
	/** Default max tokens label for budgeting UI (placeholder). */
	maxLabelLength: 256,
} as const
