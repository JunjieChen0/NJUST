/**
 * Configuration schemas for MCP settings (plan Task 6).
 * Full loader + watchers remain on {@link McpHub} until incremental split exports private helpers.
 */
export { ServerConfigSchema } from "./McpHub"
