/**
 * useRooCreditBalance - Cloud stripped. Returns null balance.
 */
export const useRooCreditBalance = (): { data: number | null; isLoading: boolean; error: unknown } => {
	return { data: null, isLoading: false, error: null }
}
